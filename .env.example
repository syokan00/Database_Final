# Database Configuration

# Option 1: Local database (default, data not shared across devices, full functionality)
# Uncomment the line below to use local database
# DATABASE_URL=postgresql://postgres:changeme@db:5432/memoluck

# Option 2: Shared demo database - READ-ONLY (can view posts from others, but cannot register/post)
# This is safe to use - read-only password can be shared publicly
# Uncomment the line below to use the shared demo database
DATABASE_URL=postgresql://readonly_demo:demo_readonly_2025@aws-1-ap-south-1.pooler.supabase.com:6543/postgres

# Option 3: Full access shared database (get connection string from project maintainer)
# For full functionality (register, post, etc.), contact the project maintainer
# DATABASE_URL=postgresql://postgres.kypqhvhbmohgoidradws:[PASSWORD]@aws-1-ap-south-1.pooler.supabase.com:6543/postgres

# Local database configuration (only used if Option 1 is selected)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=memoluck

# Redis configuration
REDIS_URL=redis://redis:6379/0

# MinIO configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme
MINIO_BUCKET=memoluck-files

# JWT Secret (use strong random string in production)
JWT_SECRET=changeme-in-production